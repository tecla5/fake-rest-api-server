{
  "name": "fake-rest-api",
  "version": "1.1.0",
  "main": "index.js",
  "repository": "https://github.com/JonJam/fake-rest-api.git",
  "author": "Jonathan Harrison",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^10.3.1",
    "chalk": "^2.4.1",
    "faker": "^4.1.0",
    "json-schema-faker": "^0.5.0-rc15",
    "json-server": "^0.13.0",
    "ts-node": "^6.1.0",
    "typescript": "^2.9.1"
  },
  "scripts": {
    "routes:map": "ts-node src/generate/routes/run.ts",
    "schemas:from-swagger": "ts-node src/generate/schemas/run.ts",
    "schemas:index": "ts-node schemas/generate-index.ts",
    "db:generate": "ts-node src/generate/data/run.ts",
    "prestart": "npm run schemas:from-swagger && npm run db:generate",
    "start:default": "json-server --watch src/db.json --port 3000",
    "start": "json-server --watch src/db.json --routes routes.json --port 3000"
  },
  "dependencies": {
    "fs-readdir-promise": "^1.0.1",
    "js-promisify": "^1.3.1",
    "js-yaml": "^3.12.0",
    "json-schema-deref": "^0.4.3",
    "json-schema-deref-local": "^0.2.1",
    "lodash": "^4.17.10",
    "lodash-inflection": "^1.5.0",
    "promisify-node": "^0.5.0",
    "template-helpers": "^0.6.7",
    "util-promisify": "^2.1.0"
  }
}
